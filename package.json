{
  "name": "ai-chess-battle",
  "version": "1.0.0",
  "private": true,
  "description": "AI Chess Battle monorepo",
  "scripts": {
    "dev": "pnpm --filter server run dev",
    "build": "pnpm --filter client run build && pnpm --filter server run build:server",
    "build:client": "pnpm --filter client run build",
    "build:server": "pnpm --filter server run build:server",
    "start": "pnpm --filter server run start",
    "lint": "pnpm --filter client run lint"
  },
  "keywords": [
    "chess",
    "ai",
    "llm"
  ],
  "author": "",
  "license": "ISC"
}
